# Cloudflare Pages 配置文件
# 文档: https://developers.cloudflare.com/pages/configuration/

name = "beichenxu-web"
compatibility_date = "2024-01-01"

# 静态站点：直接使用根目录作为输出
# 如果是构建项目（如 React/Vue），改为 "dist" 或 "build"
pages_build_output_dir = "."
